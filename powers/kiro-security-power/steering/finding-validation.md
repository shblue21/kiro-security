# Finding validation
Call `security_validate_finding` for each selected occurrence. Base conclusions on recorded code evidence and the resulting validation record; preserve uncertainty as `needs_review`.
