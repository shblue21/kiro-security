# Repository security scan
Run `security_start_scan` in `standard` mode, poll the scan, inspect coverage, list findings, validate material candidates, and report artifact paths. Do not claim completion before the reporting phase seals the manifest.
