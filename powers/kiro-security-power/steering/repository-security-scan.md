# Repository security scan
Start `security_start_scan` with `mode: "standard"`, `analysisProfile: "model"`, the selected `modelId`, and truthful `deep-worker/v2` host runtime attestation. Run exactly one all-six discovery round, submit every row receipt, perform semantic merge, then complete the canonical threat-model, validation, attack-path, writeup, and hardening assignments through the shared tail tools. A Standard round closes as saturated after its one merge; do not substitute Fast heuristic output. Do not claim completion before strict reporting seals the manifest.
